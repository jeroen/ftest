INSTDIR=../inst

PROG = hello$(WIN)
LIB = lib/hello.o

all: clean $(PROG)

$(PROG): $(LIB)
	$(F77) $(FFLAGS) $(LIB) -o $(PROG)
	mkdir -p $(INSTDIR)/bin
	cp -f $(PROG) $(INSTDIR)/bin/

clean:
	rm -Rf $(LIB) $(SHLIB) $(PROG)
